TEMPORARY FILE — delete once both laptops have their signpost set up.

Copy the content below (everything between the lines) into a NEW file called
CLAUDE.md, saved in the home folder (C:\Users\catry\CLAUDE.md) — one level UP
from this cat-co folder, not inside it.

--------------------------------------------------------------------------------
# CLAUDE.md

This is just a signpost, not the real instructions. The actual notebook for each project
now lives inside that project's own folder, so it travels between laptops on its own —
you don't have to keep this file in sync by hand.

- **Hub (the cat-co website):** instructions live in `cat-co\CLAUDE.md`. Go read that file.
  It travels between laptops via GitHub, same as the code.
- **Writing project:** instructions live in `G:\My Drive\DOBG\CLAUDE.md` (which points on to
  `README.md` in that same folder). It travels between laptops via Google Drive.

If it's unclear which project Cat wants to work on, ask her before assuming.
--------------------------------------------------------------------------------
